// Compatibility entry point for older imports.
// Use the canonical checkout so seller-scoped delivery methods are resolved
// consistently and no legacy fixed delivery fee can be applied.
export { default } from './Checkout'
